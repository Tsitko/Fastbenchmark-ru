## Переменная {} (datetime)
В этой части кода мы будем работать с переменной **{}**, которая имеет тип datetime.  
Мы сгенерируем ряд новых переменных из нее и посмотрим на их распределение относительно целевой переменной
**{}**