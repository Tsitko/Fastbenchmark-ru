## Целевая переменная {} (числовая)
В этой части кода мы будем работать с переменной **{}**, которая для нас является целевой 
(именно для предсказания значений этой переменной мы будем строить модель).  
Для начала мы посмотрим на распределение значений этой переменной.
Также мы преобразуем значения переменной, чтобы её можно было использовать при разработке ML модели