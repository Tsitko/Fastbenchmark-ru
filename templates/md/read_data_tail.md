Итак, данные загружены и разделены на обучающую и тестовую выборку.  
Мы построим модель, обучим её на обучающей выборке и попробуем предсказать значения переменной **{}** на тестовой выборке.  
Но для начала давайте посмотрим на данные и преобразуем их таким образом, чтобы их можно было использовать в ML модели.   

Посмотрим на зависимости переменных, если их не слишком много (по диагонали можно увидеть распределения переменных).  
Это может помочь нам в понимании данных:   